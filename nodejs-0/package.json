{
  "name": "nodejs-0",
  "version": "1.0.0",
  "description": "Faça um método `fibonacci` que retorne um `Array` com os primeiro números de série de *Fibonacci* até passar de *350*.",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha test/index.test.js",
    "test:watch": "mocha test/index.test.js --watch",
    "test:submit": "mocha test/submit.test.js --reporter mocha-junit-reporter --reporter-options mochaFile=./output.xml | exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "mocha-junit-reporter": "^1.18.0"
  },
  "directories": {
    "test": "test"
  },
  "dependencies": {},
  "keywords": []
}
